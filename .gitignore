# Local Claude Code config — never commit (machine-specific permissions)
.claude/settings.local.json

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/

# Node / Playwright caches (if any sub-skill uses them later)
node_modules/
package-lock.json
__pycache__/
*.pyc

# Kuaishou / Bilibili / Douyin / Xiaohongshu collection caches
private/
outputs/
